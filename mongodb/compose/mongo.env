# Used with docker compose --env-file mongodb/compose/mongo.env
# This single-shard demo has no authentication; do not put passwords here.
MONGODB_IMAGE_TAG=7.0
HOST_BIND_ADDRESS=127.0.0.1
MONGODB_HOST_PORT=27017
